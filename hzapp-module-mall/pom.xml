<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>hzapp</artifactId>
        <groupId>com.hzltd</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>hzapp-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>hzapp-module-product</module>
        <module>hzapp-module-promotion</module>
        <module>hzapp-module-trade</module>
        <module>hzapp-module-statistics</module>
        <!--
            特殊：为什么会有 hzapp-module-trade-api 呢？
                hzapp-module-promotion 和 hzapp-module-trade 之间相互循环依赖，所以抽出 hzapp-module-trade-api 模块，这样：
                1. hzapp-module-promotion 依赖 hzapp-module-trade-api
                2. hzapp-module-trade 依赖 hzapp-module-promotion
            从而不存在相互（循环）依赖，即 hzapp-module-trade => hzapp-module-promotion => hzapp-module-trade-api
         -->
        <module>hzapp-module-trade-api</module>
    </modules>

</project>
