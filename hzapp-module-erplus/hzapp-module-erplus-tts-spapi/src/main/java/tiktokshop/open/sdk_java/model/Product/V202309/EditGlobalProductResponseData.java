/*
 * tiktok shop openapi
 * sdk for apis
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package tiktokshop.open.sdk_java.model.Product.V202309;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import tiktokshop.open.sdk_java.model.Product.V202309.EditGlobalProductResponseDataGlobalSkus;
import tiktokshop.open.sdk_java.model.Product.V202309.EditGlobalProductResponseDataPublishResults;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import tiktokshop.open.sdk_java.invoke.JSON;

/**
 * EditGlobalProductResponseData
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-10-28T03:28:06.328409Z[Etc/UTC]", comments = "Generator version: 7.15.0")
public class EditGlobalProductResponseData {
  public static final String SERIALIZED_NAME_GLOBAL_SKUS = "global_skus";
  @SerializedName(SERIALIZED_NAME_GLOBAL_SKUS)
  @javax.annotation.Nullable
  private List<EditGlobalProductResponseDataGlobalSkus> globalSkus = new ArrayList<>();

  public static final String SERIALIZED_NAME_PUBLISH_RESULTS = "publish_results";
  @SerializedName(SERIALIZED_NAME_PUBLISH_RESULTS)
  @javax.annotation.Nullable
  private List<EditGlobalProductResponseDataPublishResults> publishResults = new ArrayList<>();

  public EditGlobalProductResponseData() {
  }

  public EditGlobalProductResponseData globalSkus(@javax.annotation.Nullable List<EditGlobalProductResponseDataGlobalSkus> globalSkus) {
    this.globalSkus = globalSkus;
    return this;
  }

  public EditGlobalProductResponseData addGlobalSkusItem(EditGlobalProductResponseDataGlobalSkus globalSkusItem) {
    if (this.globalSkus == null) {
      this.globalSkus = new ArrayList<>();
    }
    this.globalSkus.add(globalSkusItem);
    return this;
  }

  /**
   * A list of global Stock Keeping Units (SKUs) used to identify distinct variants of the product.
   * @return globalSkus
   */
  @javax.annotation.Nullable
  public List<EditGlobalProductResponseDataGlobalSkus> getGlobalSkus() {
    return globalSkus;
  }

  public void setGlobalSkus(@javax.annotation.Nullable List<EditGlobalProductResponseDataGlobalSkus> globalSkus) {
    this.globalSkus = globalSkus;
  }


  public EditGlobalProductResponseData publishResults(@javax.annotation.Nullable List<EditGlobalProductResponseDataPublishResults> publishResults) {
    this.publishResults = publishResults;
    return this;
  }

  public EditGlobalProductResponseData addPublishResultsItem(EditGlobalProductResponseDataPublishResults publishResultsItem) {
    if (this.publishResults == null) {
      this.publishResults = new ArrayList<>();
    }
    this.publishResults.add(publishResultsItem);
    return this;
  }

  /**
   * Results of syncing the changes in the edited product to markets where it is published.
   * @return publishResults
   */
  @javax.annotation.Nullable
  public List<EditGlobalProductResponseDataPublishResults> getPublishResults() {
    return publishResults;
  }

  public void setPublishResults(@javax.annotation.Nullable List<EditGlobalProductResponseDataPublishResults> publishResults) {
    this.publishResults = publishResults;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the EditGlobalProductResponseData instance itself
   */
  public EditGlobalProductResponseData putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    EditGlobalProductResponseData product202309EditGlobalProductResponseData = (EditGlobalProductResponseData) o;
    return Objects.equals(this.globalSkus, product202309EditGlobalProductResponseData.globalSkus) &&
        Objects.equals(this.publishResults, product202309EditGlobalProductResponseData.publishResults)&&
        Objects.equals(this.additionalProperties, product202309EditGlobalProductResponseData.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(globalSkus, publishResults, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class EditGlobalProductResponseData {\n");
    sb.append("    globalSkus: ").append(toIndentedString(globalSkus)).append("\n");
    sb.append("    publishResults: ").append(toIndentedString(publishResults)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("global_skus", "publish_results"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to EditGlobalProductResponseData
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!EditGlobalProductResponseData.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in EditGlobalProductResponseData is not found in the empty JSON string", EditGlobalProductResponseData.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (jsonObj.get("global_skus") != null && !jsonObj.get("global_skus").isJsonNull()) {
        JsonArray jsonArrayglobalSkus = jsonObj.getAsJsonArray("global_skus");
        if (jsonArrayglobalSkus != null) {
          // ensure the json data is an array
          if (!jsonObj.get("global_skus").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `global_skus` to be an array in the JSON string but got `%s`", jsonObj.get("global_skus").toString()));
          }

          // validate the optional field `global_skus` (array)
          for (int i = 0; i < jsonArrayglobalSkus.size(); i++) {
            EditGlobalProductResponseDataGlobalSkus.validateJsonElement(jsonArrayglobalSkus.get(i));
          };
        }
      }
      if (jsonObj.get("publish_results") != null && !jsonObj.get("publish_results").isJsonNull()) {
        JsonArray jsonArraypublishResults = jsonObj.getAsJsonArray("publish_results");
        if (jsonArraypublishResults != null) {
          // ensure the json data is an array
          if (!jsonObj.get("publish_results").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `publish_results` to be an array in the JSON string but got `%s`", jsonObj.get("publish_results").toString()));
          }

          // validate the optional field `publish_results` (array)
          for (int i = 0; i < jsonArraypublishResults.size(); i++) {
            EditGlobalProductResponseDataPublishResults.validateJsonElement(jsonArraypublishResults.get(i));
          };
        }
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!EditGlobalProductResponseData.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'EditGlobalProductResponseData' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<EditGlobalProductResponseData> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(EditGlobalProductResponseData.class));

       return (TypeAdapter<T>) new TypeAdapter<EditGlobalProductResponseData>() {
           @Override
           public void write(JsonWriter out, EditGlobalProductResponseData value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public EditGlobalProductResponseData read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             EditGlobalProductResponseData instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of EditGlobalProductResponseData given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of EditGlobalProductResponseData
   * @throws IOException if the JSON string is invalid with respect to EditGlobalProductResponseData
   */
  public static EditGlobalProductResponseData fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, EditGlobalProductResponseData.class);
  }

  /**
   * Convert an instance of EditGlobalProductResponseData to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

