/*
 * tiktok shop openapi
 * sdk for apis
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package tiktokshop.open.sdk_java.model.Product.V202309;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import tiktokshop.open.sdk_java.invoke.JSON;

/**
 * CheckListingPrerequisitesResponseDataShopGne
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2025-10-28T03:28:06.328409Z[Etc/UTC]", comments = "Generator version: 7.15.0")
public class CheckListingPrerequisitesResponseDataShopGne {
  public static final String SERIALIZED_NAME_EPR = "epr";
  @SerializedName(SERIALIZED_NAME_EPR)
  @javax.annotation.Nullable
  private String epr;

  public static final String SERIALIZED_NAME_PRODUCT_QUANTITY_LIMIT = "product_quantity_limit";
  @SerializedName(SERIALIZED_NAME_PRODUCT_QUANTITY_LIMIT)
  @javax.annotation.Nullable
  private String productQuantityLimit;

  public CheckListingPrerequisitesResponseDataShopGne() {
  }

  public CheckListingPrerequisitesResponseDataShopGne epr(@javax.annotation.Nullable String epr) {
    this.epr = epr;
    return this;
  }

  /**
   * Listing Prerequisites check: Whether the shop has configured extended producer responsibility(EPR) registration numbers. If it is not prepared, return a check failure and do not allow listing product.   Detailed explanation of JSON return results： - \&quot;check_result\&quot; returns the result of the prerequisite check.  - If \&quot;is_failed\&quot; is true, it indicates that the requirements for listing have not been met, and the detailed failure reasons will be returned through the \&quot;fail_reasons\&quot; field.
   * @return epr
   */
  @javax.annotation.Nullable
  public String getEpr() {
    return epr;
  }

  public void setEpr(@javax.annotation.Nullable String epr) {
    this.epr = epr;
  }


  public CheckListingPrerequisitesResponseDataShopGne productQuantityLimit(@javax.annotation.Nullable String productQuantityLimit) {
    this.productQuantityLimit = productQuantityLimit;
    return this;
  }

  /**
   * Check if the number of product listings exceeds the limit.  Detailed explanation of JSON return results： - \&quot;check_result\&quot; returns the result of the prerequisite check.  - If \&quot;is_failed\&quot; is true, it indicates that the requirements for listing have not been met, and the detailed failure reasons will be returned through the \&quot;fail_reasons\&quot; field. 
   * @return productQuantityLimit
   */
  @javax.annotation.Nullable
  public String getProductQuantityLimit() {
    return productQuantityLimit;
  }

  public void setProductQuantityLimit(@javax.annotation.Nullable String productQuantityLimit) {
    this.productQuantityLimit = productQuantityLimit;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the CheckListingPrerequisitesResponseDataShopGne instance itself
   */
  public CheckListingPrerequisitesResponseDataShopGne putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CheckListingPrerequisitesResponseDataShopGne product202309CheckListingPrerequisitesResponseDataShopGne = (CheckListingPrerequisitesResponseDataShopGne) o;
    return Objects.equals(this.epr, product202309CheckListingPrerequisitesResponseDataShopGne.epr) &&
        Objects.equals(this.productQuantityLimit, product202309CheckListingPrerequisitesResponseDataShopGne.productQuantityLimit)&&
        Objects.equals(this.additionalProperties, product202309CheckListingPrerequisitesResponseDataShopGne.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(epr, productQuantityLimit, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CheckListingPrerequisitesResponseDataShopGne {\n");
    sb.append("    epr: ").append(toIndentedString(epr)).append("\n");
    sb.append("    productQuantityLimit: ").append(toIndentedString(productQuantityLimit)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("epr", "product_quantity_limit"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(0);
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CheckListingPrerequisitesResponseDataShopGne
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CheckListingPrerequisitesResponseDataShopGne.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in CheckListingPrerequisitesResponseDataShopGne is not found in the empty JSON string", CheckListingPrerequisitesResponseDataShopGne.openapiRequiredFields.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("epr") != null && !jsonObj.get("epr").isJsonNull()) && !jsonObj.get("epr").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `epr` to be a primitive type in the JSON string but got `%s`", jsonObj.get("epr").toString()));
      }
      if ((jsonObj.get("product_quantity_limit") != null && !jsonObj.get("product_quantity_limit").isJsonNull()) && !jsonObj.get("product_quantity_limit").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `product_quantity_limit` to be a primitive type in the JSON string but got `%s`", jsonObj.get("product_quantity_limit").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CheckListingPrerequisitesResponseDataShopGne.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CheckListingPrerequisitesResponseDataShopGne' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CheckListingPrerequisitesResponseDataShopGne> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CheckListingPrerequisitesResponseDataShopGne.class));

       return (TypeAdapter<T>) new TypeAdapter<CheckListingPrerequisitesResponseDataShopGne>() {
           @Override
           public void write(JsonWriter out, CheckListingPrerequisitesResponseDataShopGne value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public CheckListingPrerequisitesResponseDataShopGne read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             CheckListingPrerequisitesResponseDataShopGne instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CheckListingPrerequisitesResponseDataShopGne given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CheckListingPrerequisitesResponseDataShopGne
   * @throws IOException if the JSON string is invalid with respect to CheckListingPrerequisitesResponseDataShopGne
   */
  public static CheckListingPrerequisitesResponseDataShopGne fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CheckListingPrerequisitesResponseDataShopGne.class);
  }

  /**
   * Convert an instance of CheckListingPrerequisitesResponseDataShopGne to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

